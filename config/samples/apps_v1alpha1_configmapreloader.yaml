apiVersion: apps.drumont/v1alpha1
kind: ConfigMapReloader
metadata:
  labels:
    app.kubernetes.io/name: configmap-reloader
    app.kubernetes.io/managed-by: kustomize
  name: configmapreloader-sample
spec:
  # TODO(user): Add fields here
